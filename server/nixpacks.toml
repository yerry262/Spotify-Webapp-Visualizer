# Nixpacks configuration for Railway deployment
# Install yt-dlp, ffmpeg, and deno (required for yt-dlp YouTube extraction)

[phases.setup]
nixPkgs = ["yt-dlp", "ffmpeg", "deno"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"
