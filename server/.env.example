# Server Configuration
PORT=3001

# yt-dlp path (optional - only needed if not in system PATH)
# YTDLP_PATH=/usr/local/bin/yt-dlp

# FFmpeg path (optional - only needed if not in system PATH)
# FFMPEG_PATH=/usr/local/bin/ffmpeg
